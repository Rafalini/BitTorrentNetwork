tracker1
  Single thread version.

  Tracker holds ip adresses of peers without duplicates.

TODO:
  --save to .cfg ip address and file names
  --read from .cfg file
  --sent 'update' requests to peers
  --delete from .cfg ip addresses and file names

tracker2
  Multi thread version.
